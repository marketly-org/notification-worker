apiVersion: v1
kind: Service
metadata:
  name: notification-worker
  namespace: marketly
  labels:
    app: notification-worker
    tier: worker
spec:
  type: ClusterIP
  selector:
    app: notification-worker
  ports:
    - name: health
      port: 8080
      targetPort: 8080
      protocol: TCP
